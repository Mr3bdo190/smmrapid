import{useState}from'react';import{useQuery,useMutation,useQueryClient}from'@tanstack/react-query';import{ShieldAlert,Search,CheckCircle2}from'lucide-react';import toast from'react-hot-toast';import{useAuth}from'../../contexts/AuthContext';import{apiFetch}from'../../lib/api';import Pagination from '../../components/Pagination';
export default function AdminSystemReports(){const{user}=useAuth();const qc=useQueryClient();const[q,setQ]=useState('');const[page,setPage]=useState(1);const rQ=useQuery({queryKey:['admin-reports',page],enabled:!!user,queryFn:async()=>{const t=await user!.getIdToken();const r=await apiFetch(`/api/admin/reports?page=${page}&pageSize=25`,user,{headers:{Authorization:`Bearer ${t}`}});if(!r.ok)throw new Error('Failed to load reports');return r.json()}});const resolve=useMutation({mutationFn:async(id:string)=>{const t=await user!.getIdToken();const r=await apiFetch(`/api/admin/reports/${id}/status`,user,{method:'PUT',headers:{'Content-Type':'application/json',Authorization:`Bearer ${t}`},body:JSON.stringify({status:'Resolved'})});if(!r.ok)throw new Error('Failed to resolve report');return r.json()},onSuccess:()=>{toast.success('Report resolved');qc.invalidateQueries({queryKey:['admin-reports']})}});const rows=(rQ.data?.items||[]).filter((r:any)=>`${r.action} ${r.location} ${r.errorReason}`.toLowerCase().includes(q.toLowerCase()));return <div className="space-y-5"><div><h3 className="text-xl font-bold flex items-center gap-2"><ShieldAlert className="w-5 h-5"/>System Reports</h3></div><div className="bg-white border rounded-xl p-4"><div className="relative max-w-xl"><Search className="absolute left-3 top-3 w-4 h-4 text-gray-400"/><input className="input-primary pl-9" placeholder="Search action, location or error" value={q} onChange={e=>setQ(e.target.value)}/></div></div><div className="bg-white border rounded-xl overflow-x-auto"><table className="min-w-[1000px] w-full"><thead className="bg-gray-50"><tr>{['Date','Action','Location','Error / Reason','Status','Action'].map(h=><th key={h} className="px-4 py-3 text-left text-xs text-gray-500 uppercase">{h}</th>)}</tr></thead><tbody className="divide-y">{rows.map((r:any)=><tr key={r.id}><td className="px-4 py-3 text-xs">{new Date(r.createdAt).toLocaleString()}</td><td className="px-4 py-3 text-sm">{r.action}</td><td className="px-4 py-3 text-sm">{r.location}</td><td className="px-4 py-3 text-sm text-red-600">{r.errorReason}</td><td className="px-4 py-3">{r.status}</td><td className="px-4 py-3">{r.status!=='Resolved'&&<button className="text-emerald-600" onClick={()=>resolve.mutate(r.id)}><CheckCircle2 className="w-4 h-4 inline mr-1"/>Resolve</button>}</td></tr>)}</tbody></table><Pagination page={rQ.data?.page||page} totalPages={rQ.data?.totalPages||1} onPage={setPage}/></div></div>}
